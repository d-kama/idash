{
  "name": "@idash/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "test": "vitest run",
    "test:update": "vitest run -u"
  },
  "dependencies": {
    "aws-cdk-lib": "^2",
    "constructs": "^10"
  },
  "devDependencies": {
    "@types/node": "^22",
    "aws-cdk": "^2",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3"
  }
}
