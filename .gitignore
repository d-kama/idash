# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# uv cache（lockfile（uv.lock）はコミット対象なので無視しない）
.uv/

# ---- Node / TypeScript ----
node_modules/
.pnpm-store/
*.tsbuildinfo
apps/frontend/dist/
# pnpm-lock.yaml はコミット対象なので無視しない

# 型生成の生成物（Pydantic → OpenAPI → TS。task gen-types が生成・非コミット。
# Pydantic を単一の真実源に保ち、生成物の鮮度ドリフトを構造的に排除する）
apps/frontend/openapi.json
apps/frontend/src/api/generated/

# ---- ローカルデータストア ----
# task bff のローカル BFF が read する Parquet（個人資産データを含み得るため追跡しない）
/data/

# ---- CDK ----
cdk.out/
*.cdk.staging/

# ---- 環境変数・機密 ----
.env
.env.*
!.env.example
# ローカル検証ランナー（scripts/run_collect_local.py）の認証情報 JSON
*.local.json
# 同ランナーが書く失敗時ページ（個人情報を含み得るため追跡しない）
/errorpages/

# ---- OS / エディタ ----
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
